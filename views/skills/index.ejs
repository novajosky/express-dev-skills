<%- include('../partials/header') %>

<h1>Skills</h1>
<ul>
  <% todos.forEach(function(s) { %>
    <li>
      <a href="/skills/<%= t.id %>">
        <%= s.skill %>
      </a>
        - 
      <%= s.done ? 'done' : 'not done' %>
    </li>
  <% }); %>
</ul>

<%- include('../partials/footer') %>